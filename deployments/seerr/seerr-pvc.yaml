apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seerr-data
  namespace: servarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path

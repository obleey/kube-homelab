apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qb-data
  namespace: servarr
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
